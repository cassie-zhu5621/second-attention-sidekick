# Dataset photos (captured at runtime — don't commit)
dataset/
*.jpg
*.jpeg

# Python
__pycache__/
*.pyc
.venv/
venv/

# macOS
.DS_Store

# Arduino build output
build/
*.bin

# new system outputs
**/_perception/
live_captures/
*.png

# Model weights — downloadable, too big for git (GitHub rejects >100MB)
*.pt
*.pth
*.onnx
config_gate/weights/
config_gate/yolov8s-world.pt

# Runtime feed / logs (config_gate)
config_gate/feed/
*.jsonl

# recordings & perception outputs
*.mp4
**/_perception*/

# --- exceptions: paper data & study artifacts MUST be committed ---
!config_gate/results/**
!config_gate/scenes/*.jpg
*.task
config_gate/context.txt
